import React from 'react';

// Component disabled as per user request to remove floating notifications.
// Notifications are now fully handled via the Notification Bell in the Chatbot header.
export const NotificationSystem: React.FC = () => {
  return null;
};
