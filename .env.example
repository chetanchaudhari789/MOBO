# Root env (monorepo)
#
# The Next.js portals proxy all requests to `/api/*` to this backend base URL.
#
# In local dev, the backend defaults to http://localhost:8080.
NEXT_PUBLIC_API_PROXY_TARGET=http://localhost:8080
