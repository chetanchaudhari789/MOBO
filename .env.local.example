# Frontend-only environment (Vite)
# Optional: override dev proxy target for /api
VITE_API_PROXY_TARGET=http://localhost:8080

# Optional: explicit API base URL (defaults to /api)
# VITE_API_URL=/api
